# advanced-regex-replacer
A VSCode extension for regex-based search and programmable replacement across files and folders. Features a intuitive sidebar panel with searchable scopes, match previews, selectable replacements, and support for multiline searches and JS-eval replacements (e.g., attack=($${x1 * 2}$$)).
